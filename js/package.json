{
    "name": "@mircle/group-list",
    "private": true,
    "dependencies": {
        "flarum": "^0.1.0-beta.16",
        "fof/upload": "*",
        "flarum-webpack-config": "^1.0",
        "webpack": "^4.26.0",
        "webpack-cli": "^3.0.7"
    },
    "scripts": {
        "dev": "webpack --mode development --watch",
        "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider webpack --mode production"
    },
    "devDependencies": {
        "cross-env": "^7.0.3"
    }
}
